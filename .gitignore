~$*.xlsm
~$*.xlsx
.~lock*
Thumbs.db
desktop.ini

# Contains real email addresses, server names, and computer names.
# This repo is public - never track this file here.
src/LocalConfig.bas

# Product planning notes with real customer/business specifics
# (destination countries, batch/inventory codes, grading data).
# This repo is public - never track this file here.
ROADMAP.md

# Old dev-history workbook snapshots, predating the LocalConfig split -
# almost certainly contain real emails/server/computer names directly in
# their VBA, not behind any gitignored module. This repo is public - never
# track these here.
Previous versions/
